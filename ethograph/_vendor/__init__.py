"""Vendored third-party code bundled with ethograph.

Sub-packages here are copied verbatim (with minimal, documented edits) from
upstream projects that are not published on PyPI, so that ``pip install`` can
provide them without a manual git install. Each sub-package keeps its original
LICENSE file.
"""
